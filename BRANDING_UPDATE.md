# 品牌和内容更新总结

## 平台名称更新

### 修改内容
- ✅ 平台名称：**情感语音平台** → **本周之声**
- ✅ 模型名称：**random-embedding** → **codec**

## 详细修改列表

### 1. 前端界面更新

#### Layout 组件 (`frontend/src/components/Layout.tsx`)
- **顶部导航栏**：情感语音平台 → 本周之声
- **页面底部**：情感人格化语音平台 ©2024 → 本周之声 ©2024

#### Home 页面 (`frontend/src/pages/Home.tsx`)
- **页面标题**：语音角色平台 → 本周之声 - 语音角色平台

#### HTML 标题 (`frontend/index.html`)
- **浏览器标签页**：frontend → 本周之声 - 语音角色平台

#### CreateVoice 页面 (`frontend/src/pages/CreateVoice.tsx`)
- **模型参数**：random-embedding → codec

### 2. TTS 示例展示更新 (`frontend/src/pages/TTSGeneration.tsx`)

#### 原始音频卡片
- **说话人**：MCY
- **时长**：约 5 秒
- **文本内容**：
  ```
  "欢迎来到周周黑客松，来开始你的表演吧"
  ```
- **样式**：蓝色左边框，浅灰背景

#### 克隆音频卡片
- **克隆模型**：Codec 模型（原：语音克隆系统）
- **效果**：高度还原
- **文本内容**：
  ```
  "一个让克隆声音真正活起来的智能人格语音生态：
  带情绪、长期记忆、互动体验与场景化价值。"
  ```
- **样式**：绿色左边框，浅绿背景

### 3. 后端更新 (`backend/src/services/voiceServiceMemory.ts`)
- **默认模型**：random-embedding → codec
- **元数据类型**：random-embedding → codec-model

## 视觉效果

### 文本展示框样式

**原始音频文本框：**
- 背景：#fafafa（浅灰）
- 左边框：3px 蓝色 (#1890ff)
- 字体大小：13px

**克隆音频文本框：**
- 背景：#f6ffed（浅绿）
- 左边框：3px 绿色 (#52c41a)
- 字体大小：13px
- 行高：1.6（更好的可读性）

## 品牌一致性

所有页面现在统一使用 **"本周之声"** 作为平台名称：
- ✅ 顶部导航栏
- ✅ 页面标题
- ✅ 浏览器标签页
- ✅ 页面底部版权信息

## 技术细节

### 模型标识
- 前端创建时使用：`model: 'codec'`
- 后端默认值：`model || 'codec'`
- 元数据类型：`type: 'codec-model'`

### 音频文件
- 原始音频：`/mcy.wav`
- 克隆音频：`/mcy-clone.wav`
- 位置：`frontend/public/`

## 访问方式

1. 打开浏览器：http://localhost:3000
2. 查看顶部导航栏：显示 "本周之声"
3. 进入 TTS 生成页面
4. 查看示例展示区域，包含文本内容

## 效果预览

```
┌─────────────────────────────────────────────────┐
│  本周之声                                        │  ← 顶部导航
├─────────────────────────────────────────────────┤
│  效果示例                                        │
│  ┌──────────────────┐  ┌──────────────────┐   │
│  │ 原始音频          │  │ 克隆后音频        │   │
│  │ 说话人: MCY       │  │ 模型: Codec      │   │
│  │ 时长: 约5秒       │  │ 效果: 高度还原    │   │
│  │ ┌──────────────┐ │  │ ┌──────────────┐ │   │
│  │ │ 文本: "欢迎来 │ │  │ │ 文本: "一个让 │ │   │
│  │ │ 到周周黑客松" │ │  │ │ 克隆声音真正" │ │   │
│  │ └──────────────┘ │  │ └──────────────┘ │   │
│  │ [音频播放器]     │  │ [音频播放器]     │   │
│  └──────────────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────┘
```

## 更新时间
2024年（根据系统时间）
